<% @users.each do |user| %>
	<% unless current_user == user %>
		<%= link_to "#{user.firstname} #{user.lastname}", user %> | <%= link_to "Add Friend", new_friend_request_path(id: user.id) %><p>
	<% end %>
<% end %>